@model TransrodenProyecto.Calculadora.CalcEntrega

@using TransrodenProyecto.Models
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container mt-5">
    <h3>Estas seguro de eliminar esta entrega?</h3>
    <br>
    <div>
        <h2 class="text-center">Borrar Entrega</h2>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Tipo)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Tipo)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Tarifa)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Tarifa)
            </dd>

        </dl>
        <br>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Borrar" class="btn btn-danger" /> |
                <a href="@Url.Action("AdminCalculadora", "AdminCalculadora")" class="btn btn-primary btn-sm">Regresar</a>
            </div>
        }
    </div>
</div>
